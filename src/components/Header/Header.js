import React from 'react';
import "./Header.css";

const Header = () => (
  <div className="jumbotron jumbotron-fluid">
    <h1>
      <span>
        click each character to earn points. click the same character twice, and
        you lose.
      </span>
    </h1>
  </div>
);

export default Header;